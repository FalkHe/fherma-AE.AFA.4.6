# Local environment — never commit; use .env.dist as the template.
.env

# Local-only overrides (e.g. compose.local.yaml) — never commit.
*.local.yaml

# Retained ingestion payloads and generated media (DATA_DIR / MEDIA_DIR).
backend/var/

# Python
__pycache__/
*.py[cod]
.venv/

# Playwright
.playwright-cli

# VS Code
.vscode

# AC Report
/ac-report.*
# Playwright MCP session artefacts (screenshots, snapshots, console logs)
.playwright-mcp/

# locals
*.local.md
*.local.yml
*.local.json